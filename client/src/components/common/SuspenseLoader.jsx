import {Box, Typography, CircularProgress} from '@mui/material';


const SuspenseLoader = () => {
    <Box>
        <CircularProgress />
        <Typography style={{color: '#000000'}}>Loading...</Typography>
    </Box>
};

export default SuspenseLoader;
